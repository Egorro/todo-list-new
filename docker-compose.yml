services:
  backend:
    build:
      context: ./server
    environment:
      NODE_ENV: production
      PORT: 3000
      CORS_ORIGIN: http://localhost:8080
    expose:
      - "3000"

  frontend:
    build:
      context: .
    ports:
      - "8080:80"
    depends_on:
      - backend
